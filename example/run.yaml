name: run
commands:
  - name: alias
    # run: "[git, status]"
    run: [git, status]

  - name: legacy
    run: "[git, status]"

  - name: macro
    run: "$(pwd)"
    # run: "$fish(pwd) ||| $chdir(/tmp)"

  - name: script ARG
    run: |
      #!/usr/bin/env bash
      echo "$@"
    completion:
      positional:
        - [one, two]
