name: run
commands:
  - name: alias
    # run: "[git, status]"
    run: [git, status]

  - name: legacy
    run: "[git, status]"

  - name: macro
    run: "$(pwd)"
    # run: "$fish(pwd) ||| $chdir(/tmp)"

  - name: script
    run: |
      #!/usr/bin/env bash
      echo "$@"
    completion:
      positional:
        - [one, two]
